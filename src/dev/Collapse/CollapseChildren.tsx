import * as React from 'react';
import {
  Text, View, StyleSheet,
} from 'react-native';

interface CollapseChildrenProps {
  children: React
}

const CollapseChildren = (props: CollapseChildrenProps) => {
  const { } = props;
};

export default CollapseChildren;

const styles = StyleSheet.create({
  container: {},
});
